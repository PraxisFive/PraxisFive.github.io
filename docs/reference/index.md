# Reference

Reference pages are for lookup rather than reading straight through:
definitions, specifications, and configuration details.

!!! note "Starter content"
    Add reference pages in `docs/reference/` and list them in `zensical.toml`.
